[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && node --experimental-global-webcrypto --import tsx/esm src/index.ts"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
